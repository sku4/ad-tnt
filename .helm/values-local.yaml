replicaCount: 1
imageMaster:
  repository: registry.digitalocean.com/sku4/repository
  pullPolicy: IfNotPresent
  tag: ""
imageReplica:
  repository: registry.digitalocean.com/sku4/repository
  pullPolicy: IfNotPresent
  tag: ""

imagePullSecrets:
  - name: regcred
nameOverride: "ad-tnt"
fullnameOverride: "ad-tnt"

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels:
  app: ad-tnt

podSecurityContext: {}
securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

serviceMaster:
  create: true
  type: ClusterIP
  externalPort: 3301
  internalPort: 3301

serviceReplica:
  create: true
  type: ClusterIP
  externalPort: 3302
  internalPort: 3301

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

#livenessProbe:
#  httpGet:
#    path: /
#    port: http
#readinessProbe:
#  httpGet:
#    path: /
#    port: http

strategy:
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 1
  type: RollingUpdate

volumes:
  - name: ad-tnt-storage
    persistentVolumeClaim:
      claimName: ad-tnt
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

volumeMountsMaster:
  - name: ad-tnt-storage
    mountPath: /usr/share/tarantool/ad/master
    subPath: master

volumeMountsReplica:
  - name: ad-tnt-storage
    mountPath: /usr/share/tarantool/ad/replica
    subPath: replica

envMaster:
  - name: TT_INSTANCE_NAME
    value: "master"
  - name: TT_INSTANCE_PORT
    value: "3301"
  - name: TT_REPLICATION
    value: "ad-tnt-master.ad-prod.svc.cluster.local,ad-tnt-replica.ad-prod.svc.cluster.local"

envReplica:
  - name: TT_INSTANCE_NAME
    value: "replica"
  - name: TT_INSTANCE_PORT
    value: "3301"
  - name: TT_REPLICATION
    value: "ad-tnt-master.ad-prod.svc.cluster.local,ad-tnt-replica.ad-prod.svc.cluster.local"
  - name: TT_INSTANCE_RO
    value: "true"

nodeSelector: {}

tolerations: []

affinity: {}

persistentVolume:
  path: /mnt/storage
  nodeAffinity:
    hostname:
      - multitest
  storage: 500Mi

storageClass:
  create: true
  name: ad-tnt-storage
